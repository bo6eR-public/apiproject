apiVersion: v1
kind: Service

metadata:
  name: my-app-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: my-app

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      nodePort: {{ .Values.service.nodePort }}
